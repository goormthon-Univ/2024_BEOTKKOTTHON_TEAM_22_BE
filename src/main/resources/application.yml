server:
  servlet:
    encoding:
      charset: utf-8
      force: true
  port: 5000
spring:
  datasource:
    url: jdbc:mariadb://localhost:3300/fairy
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 8155103283
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      default_batch_fetch_size: 100
    open-in-view: false
  web:
    resources:
      add-mappings: false
  cloud:
    aws:
      stack.auto: false








